name: Deploy to Development

on:
  push:
    branches:
      - main  # Sesuaikan dengan branch utama

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Setup SSH Connection
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Deploy Code to cPanel
      run: |
        ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "
          cd ${{ secrets.DEPLOY_PATH }} &&
          git pull origin main &&
          exit"

    - name: Create an Issue for Notification
      uses: peter-evans/create-issue@v3
      with:
        title: 'Notifikasi Deployment'
        body: |
          🚀 **Deployment Selesai di Development**:
          - **Branch**: `main`
          - **Commit Terakhir**: `${{ github.sha }}`
          - **Link**: [Lihat di Development](http://dev.esensia.co.id)
          
          ✅ **Deployment ke server development selesai**. Silakan cek hasilnya.

        labels: "deployment"

    - name: Notify Contributors via Issue Comment
      uses: peter-evans/create-or-update-comment@v3
      with:
        issue-number: 1  # Ganti dengan nomor issue tempat komentar akan diposting
        body: |
          🚀 **Deployment Selesai di Development**:
          - **Branch**: `main`
          - **Commit Terakhir**: `${{ github.sha }}`
          - **Link**: [Lihat di Development](http://dev.esensia.co.id)
          
          ✅ **Deployment ke server development selesai**. Silakan cek hasilnya.
